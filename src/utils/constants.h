// Define constantes para o número máximo e mínimo que pode ser chutado
#define MAX_ACCEPTED_NUMBER 20
#define MIN_ACCEPTED_NUMBER 0

// Informa a quantidade máxima de números que o computador poderá escolher
// Possui um número a mais para definir o final que é número nulo
#define POSSIBLE_NUMBERS_ARRAY_SIZE 22
#define NULL_NUMBER 99

// Define variáveis para tornar mais legível a identificação do usuário e do pc
#define USER 'u'
#define IA 'c'

// Define a linguagem padrão que será usada no setLocale()
extern const char DEFAULT_LOCALE[15];

// Define o caminho padrão e nome do arquivo de save do jogo
extern const char SCORE_SAVE_PATH[25];
extern const char SCORE_SAVE_FULL_FILE_PATH[40];
