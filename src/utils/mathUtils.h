/*
 * Define a função para gerar números aleatórios.
 * Ele recebe dois valores para ser mais flexível e
 * permitir gerar valores entre uma faixa
 */
int createRandomNumber(const int baseValue, const int maxValue);
